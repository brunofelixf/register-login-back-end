version: "3.9"

services:

  db:
    image: postgres:latest
    restart: always
    environment:
      POSTGRES_USER: bruno
      POSTGRES_PASSWORD: 123
      POSTGRES_DB: db1
    ports:
      - "5432:5432"